import 'package:flutter/material.dart';

class Textfieldbara {
  String? controller;
  String? labelText;
  String? obscureText;
  IconButton? suffixIcon;
  Textfieldbara(
      {this.controller, this.labelText, this.obscureText, this.suffixIcon});
}
